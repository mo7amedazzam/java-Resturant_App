<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="relative.alaqsa.edu.restaurant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RESTAURANT">


        <activity android:name=".OpenMeals">
        </activity>
        <activity android:name=".SignUp_Activity" />
        <activity android:name=".SignIn_Activity" />
        <activity android:name=".ScreenMeal" >

        </activity>
        <activity android:name=".Application_Interface" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>